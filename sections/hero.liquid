<section class="red-sea-hero-section">
    <div class="red-sea-hero-container">
      <h1 class="red-sea-hero-title">{{ section.settings.hero_title }}</h1>
      <p class="red-sea-hero-subtitle">{{ section.settings.hero_subtitle }}</p>
      <a href="{{ section.settings.hero_button_link }}" class="red-sea-hero-button">
        {{ section.settings.hero_button_text }}
      </a>
    </div>
  </section>
  